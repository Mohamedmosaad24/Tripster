@model BLTripster.ViewModels.RoomVM
@{
    Layout = "_DashLayout";
    ViewData["Title"] = "Edit Room";
}

<div class="container-fluid px-4">

    <!-- Page Heading -->
    <div class="d-sm-flex align-items-center justify-content-between mb-4">
        <h3 class="h3 mb-0 text-gray-800">
            <i class="fas fa-pen text-warning mr-2"></i>
            Edit Room
        </h3>
    </div>

    <!-- Card -->
    <div class="card shadow mb-4">

        <div class="card-header py-3">
            <h6 class="m-0 font-weight-bold text-primary">
                Update Room Information
            </h6>
        </div>

        <div class="card-body p-4">

            <form asp-action="EditRoom"
                  asp-controller="Dashboard"
                  method="post" enctype="multipart/form-data">

                @Html.AntiForgeryToken()

                <input type="hidden" asp-for="Id" />
                <input type="hidden" asp-for="HotelId" />

                <!-- Room Image -->
                <div class="form-group">
                    <label class="font-weight-bold">Room Image</label>
                    <div class="d-flex align-items-center gap-3">
                        @if (!string.IsNullOrEmpty(Model.ImageUrl))
                        {
                            <img id="roomEditPreview" src="@Model.ImageUrl" alt="Current" class="rounded" width="80" height="80" style="object-fit:cover; border: 2px solid #e2e8f0;" />
                        }
                        else
                        {
                            <img id="roomEditPreview" alt="Preview" class="rounded" width="80" height="80" style="object-fit:cover; border: 2px solid #e2e8f0; display:none;" />
                        }
                        <input type="file" name="RoomImage" class="form-control" accept="image/*" onchange="previewImg(this, 'roomEditPreview')" />
                    </div>
                    <small class="text-muted">Choose a new image from your computer (JPG, PNG, GIF)</small>
                </div>

                <!-- Room Type -->
                <div class="form-group">
                    <label asp-for="RoomType" class="font-weight-bold"></label>
                    <input asp-for="RoomType" class="form-control" />
                    <span asp-validation-for="RoomType" class="text-danger"></span>
                </div>

                <!-- Capacity -->
                <div class="form-group">
                    <label asp-for="Capacity" class="font-weight-bold"></label>
                    <input asp-for="Capacity" class="form-control" />
                </div>

                <!-- Price -->
                <div class="form-group">
                    <label asp-for="Price" class="font-weight-bold"></label>
                    <input asp-for="Price" class="form-control" />
                </div>

                <!-- Buttons -->
                <div class="mt-4">

                    <button type="submit"
                            class="btn btn-warning px-4 shadow-sm">
                        <i class="fas fa-save mr-1"></i>
                        Update
                    </button>

                    <a asp-action="Rooms" asp-controller="Dashboard"
                       class="btn btn-secondary px-4 ml-2">
                        <i class="fas fa-arrow-left mr-1"></i>
                        Back
                    </a>

                </div>

            </form>

        </div>
    </div>

</div>

<script>
    function previewImg(input, previewId) {
        var preview = document.getElementById(previewId);
        if (input.files && input.files[0]) {
            var reader = new FileReader();
            reader.onload = function(e) {
                preview.src = e.target.result;
                preview.style.display = 'block';
            }
            reader.readAsDataURL(input.files[0]);
        }
    }
</script>

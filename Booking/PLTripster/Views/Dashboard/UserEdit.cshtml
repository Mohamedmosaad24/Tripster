@using DATripster.Entities
@model User
@{
    Layout = "_DashLayout";
    ViewData["Title"] = "Edit User";
}

<div class="container-fluid px-4">
    <h2 class="mb-4">Edit User</h2>
    <div class="card shadow mb-4">
        <div class="card-body">
            <form asp-action="Edit" asp-controller="Dashboard" method="post" enctype="multipart/form-data">
                <input type="hidden" asp-for="Id" />
                <input type="hidden" asp-for="ImageUrl" />
                <div asp-validation-summary="All" class="text-danger mb-3"></div>

                <div class="form-group mb-3">
                    <label asp-for="Name" class="form-label"></label>
                    <input asp-for="Name" class="form-control" required />
                    <span asp-validation-for="Name" class="text-danger"></span>
                </div>
                <div class="form-group mb-3">
                    <label asp-for="Email" class="form-label"></label>
                    <input asp-for="Email" type="email" class="form-control" />
                    <span asp-validation-for="Email" class="text-danger"></span>
                </div>
                <div class="form-group mb-3">
                    <label asp-for="Location" class="form-label"></label>
                    <input asp-for="Location" class="form-control" />
                </div>
                <div class="form-group mb-3">
                    <label asp-for="Nationality" class="form-label"></label>
                    <input asp-for="Nationality" class="form-control" />
                </div>
                <div class="form-group mb-3">
                    <label asp-for="DateOfBirth" class="form-label">Date of Birth</label>
                    <input asp-for="DateOfBirth" type="date" class="form-control" />
                </div>
                <div class="form-group mb-3">
                    <label class="form-label">Profile Image</label>
                    <div class="d-flex align-items-center gap-3">
                        @if (!string.IsNullOrEmpty(Model.ImageUrl))
                        {
                            <img id="userEditPreview" src="@Model.ImageUrl" alt="Current" class="rounded" width="80" height="80" style="object-fit:cover; border: 2px solid #e2e8f0;" />
                        }
                        else
                        {
                            <img id="userEditPreview" src="/images/default-user.png" alt="Preview" class="rounded" width="80" height="80" style="object-fit:cover; border: 2px solid #e2e8f0; display:none;" />
                        }
                        <input type="file" name="ProfileImage" class="form-control" accept="image/*" onchange="previewImg(this, 'userEditPreview')" />
                    </div>
                    <small class="text-muted">Choose a new image from your computer (JPG, PNG, GIF)</small>
                </div>
                <div class="mt-3">
                    <button type="submit" class="btn btn-warning">Save</button>
                    <a asp-action="AllUsers" asp-controller="Dashboard" class="btn btn-secondary">Cancel</a>
                    <a asp-action="Details" asp-controller="Dashboard" asp-route-id="@Model.Id" class="btn btn-info">Details</a>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
        function previewImg(input, previewId) {
            var preview = document.getElementById(previewId);
            if (input.files && input.files[0]) {
                var reader = new FileReader();
                reader.onload = function(e) {
                    preview.src = e.target.result;
                    preview.style.display = 'block';
                }
                reader.readAsDataURL(input.files[0]);
            }
        }
    </script>
}
